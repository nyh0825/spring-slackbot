server:
  port: 8080
  servlet:
    context-path: /slackbot

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/slackbot?useSSL=false&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: qq4356649

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console
